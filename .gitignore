# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Intellij
.idea/

# Vendored upstream reference materials kept locally for validation
# (e.g. NOAA's WMMHR2025 Linux distribution under assets/wmm/).
assets/
